# 008.md - Complete Allowed Updates Configuration and v0.2 Integration Hardening

## Current Task
Finalize v0.2 configuration behavior for `allowed_updates` and perform integration hardening across the newly added v0.2 capabilities.

## Scope
- Ensure `allowed_updates=[]` explicitly sends an empty list and resets Telegram-side filtering to the official default behavior.
- Ensure `allowed_updates=None` omits the parameter and preserves previous Telegram-side configuration.
- Ensure explicit lists are passed exactly as configured.
- Add cross-feature integration tests using fake update sources and mocked HTTP calls.
- Verify logs, lifecycle behavior, cancellation, queue behavior, and v0.2 namespace consistency.

## Expected Deliverables
- Production code fixes for allowed updates configuration, if needed.
- Integration tests for polling request parameters and v0.2 namespaces working together.
- Regression tests for no token leakage in logs/status/errors.
- A final v0.2 test run under external timeout.

## Mandatory Implementation Requirements
- Add complete logging for debug visibility while redacting secrets.
- Follow test-driven development: write failing tests before implementation.
- First understand the relevant classes and functions; tests must be feature-driven.
- Cover boundary issues and edge cases explicitly.
- Keep code robust, defensive, and maintainable.
- Prevent deadlocks, memory leaks, CPU busy loops, and unbounded resource growth.
- Every async test must use timeout control to avoid infinite blocking.
- Run test commands with an external timeout as a second safety layer.
- Commit changes to git only after all tests pass.

## Prompt
You are implementing task 008 for the TG Bot event-driven SDK v0.2. Start by reading the polling configuration, transport request construction, Bot lifecycle, event engine, and all v0.2 namespace changes to understand how the pieces interact. Use test-driven development: write behavior-focused tests proving the three `allowed_updates` semantics: empty list is sent, `None` is omitted, and explicit lists are passed unchanged. Add integration-style tests with fake update sources and mocked HTTP calls for lifecycle, cancellation, queue draining, system events, and representative v0.2 action methods. Add comprehensive logging checks where appropriate, ensuring logs help debugging but never leak full bot tokens. Pay close attention to edge cases, no deadlocks, no memory leaks, no CPU busy loops, clean cancellation, and bounded queues. Every async test must have timeout control, and every external test command must also be wrapped in a timeout as a second safety layer. Run targeted tests and the full test suite with timeout protection. After all tests pass, commit the changes to git with a clear conventional commit message.
